/* TESTEA LA FUNCIONALIDAD DEL HOME */
/*
describe('Prueba de Usuarios', () => {
    it('Carga la lista de usuarios', () => {
      cy.request('GET', 'http://localhost:3001/users').then((response) => {
        expect(response.status).to.eq(200);
        expect(response.body).to.be.an('array'); // Asegura que devuelve un array
      });
    });
  });
*/